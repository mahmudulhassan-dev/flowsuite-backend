# FlowSuite Enterprise — Node.js TypeScript Backend

This is the Node.js TypeScript backend for **FlowSuite Omnichannel AI Platform**.

## GitHub Push Instructions for `@mahmudulhassan-dev`

```bash
cd g:/flowsuite/backend

git init
git add .
git commit -m "feat: Initial FlowSuite Node.js Backend Setup"
git remote add origin https://github.com/mahmudulhassan-dev/flowsuite-backend.git
git branch -M main
git push -u origin main
```
